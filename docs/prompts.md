# Prompts log

> p1: update specs to have 5 stars ranking system for importance rather than using Level enum

> p2
```
among these options, which sounds best

* option1 (current): Category picker handling both select and edit categories, + separate Source picker
* option2: Category picker handling both topic/project and source selection. Separate Category Editor
* option3
```

> report specs mismatch
> check code conformity with specs. 
> report any misalignment between current code impl and [data model](specs/model.md)

> check differences between @docs/specs/ui.md specs and current implementation (ignore planned features)