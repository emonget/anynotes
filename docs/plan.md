MVP: basic matching
- allow user setting editables categories (Topics, Projects)
- copy all local content along with prompt instructions to match items withing categories
(scope restricted to)
-  paste data to update items based on timestamp with matched categories. check data integrity. all previewing changes to be imported before updating local storage (git diff like?)

scope:
- match items solely based on user defined categories 
- rematch all local content items each time

MVP2:
- allow inputing API key to call directly AI service
- preview categories matchings before updating local data